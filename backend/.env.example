# Con Docker Compose (postgres = nome del servizio):
DATABASE_URL=postgresql://trailbraizer:trailbraizer@postgres:5432/trailbraizer

# Con PostgreSQL locale (senza Docker):
# DATABASE_URL=postgresql://user:password@localhost:5432/trailbraizer

SECRET_KEY=change_me_to_a_random_secret_key_min_32_chars
ACCESS_TOKEN_EXPIRE_MINUTES=10080
